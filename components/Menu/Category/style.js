import createStyles from 'styles/base';

const style = createStyles({
    menuItem: {
        opacity: 0.5,
        marginLeft: 10
    },
    circle: {
        
    },
    category: {
        flexDirection: "row",
        alignItems: "center",
        marginTop: 30
    }
});

export default style;