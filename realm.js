
/*import Realm from 'realm';

class Event extends Realm.Object {}
Event.schema = {
    name: 'Event',
    properties: {
      name: 'string',
      time: 'string?',
      note: 'string?',
      label: 'string'
    },
};

class Date extends Realm.Object {}
Date.schema = {
    name: 'Date',
    properties: {
      date: 'date',
      events: 'Event[]'
    }
};

export default new Realm({schema: [Event, Date]});*/